<div class="row">
	<div class="col-md-4">
		<nav class="list-group">
			{{#each}}
				{{#link-to 'user' this class="list-group-item"}}{{displayName}}{{/link-to}}
			{{/each}}
		</nav>
		{{#link-to 'users.new' class="btn btn-primary btn-block"}}New User{{/link-to}}
	</div>
	<div class="col-md-8">
		{{outlet}}
	</div>
</div>

