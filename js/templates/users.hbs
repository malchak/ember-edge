<div class="row">
	<div class="col-md-4">
		<nav class="list-group">
			{{#each}}
				{{#link-to 'user' this class="list-group-item"}}{{displayName}}{{/link-to}}
			{{/each}}
		</nav>
	</div>
	<div class="col-md-8">
		{{outlet}}
	</div>
</div>

